# builtin_unset.c 疑似コード

## 関数: builtin_unset
- **概要**:
  - 指定された環境変数を削除する。
- **引数**:
  - `args`: コマンド引数の配列
  - `data`: シェルの主要データ構造体
- **処理**:
  1. 引数がない場合 (`!args[1]`)、`0` を返す。
  2. 引数を順に処理するループ (`i = 1` から):
     - `unset_env_value` を呼び出し、指定されたキーの環境変数を削除する。
     - 次の引数へ進む。
  3. `0` を返す。
